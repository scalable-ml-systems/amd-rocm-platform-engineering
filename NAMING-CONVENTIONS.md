Folders and Markdown files:
lowercase-kebab-case

Python modules and variables:
lowercase_snake_case

Python classes:
PascalCase

Shell scripts:
verb-descriptive-purpose.sh

Experiment IDs:
exp-p<phase>-<sequence>-<descriptive-name>

Hardware profiles:
hw-<gpu>-<gpu-count>-<partition>-<interconnect>

Software stacks:
sw-<engine>-<backend>-<precision>-r<revision>

Failures:
failure-<sequence>-<descriptive-name>.md

Benchmark runs:
generated timestamp + Git revision

Metric fields:
always include units

EXAMPLES:

exp-p02-003-pinned-memory-transfer

hw-mi300x-8x-spx-nps1-xgmi

sw-vllm-aiter-fp8-r01

failure-003-rccl-bandwidth-asymmetry.md

collect-host-evidence.sh

latency_millisecond
